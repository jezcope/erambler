default:
  image: jezcope/erambler-nikola:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

pages:
  script:
    - nikola version
    - nikola build
  artifacts:
    paths:
      - public
  only:
    - master
